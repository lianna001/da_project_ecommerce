USE ROLE ACCOUNTADMIN;

-- 테이블을 대문자 이름으로 다시 생성
CREATE OR REPLACE TABLE SNOWFLAKE_LEARNING_DB.PUBLIC.RAW_PURCHASE_DATA (
    PURCHASE_ID VARCHAR,
    AMOUNT NUMBER,
    PURCHASE_DATE DATE
);
 --오픈 권한 생성
GRANT ALL PRIVILEGES ON TABLE SNOWFLAKE_LEARNING_DB.PUBLIC.RAW_PURCHASE_DATA TO ROLE PUBLIC;
